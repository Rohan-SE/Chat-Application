<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/daisyui@4.12.10/dist/full.min.css" rel="stylesheet" type="text/css" />
    <title>Login</title>
</head>
<body class="w-full h-screen flex justify-center bg-white">
    <% if(error.length>0) { %>
        <div class="absolute text-white bg-red-700 p-4 left-1/3 text-2xl w-96 text-center">
            <%= error %>
        </div>
    <% } %>
    <div class="mt-10 flex flex-col gap-4 items-center bg-red-500 border-2 border-black rounded-md h-3/4 w-1/2 text-white">
     <h1 class="text-4xl mt-3 font-semibold">Login</h1>
     <form class="flex mt-10 flex-col gap-4 justify-center items-center" action="/login" method="post">
         <label for="" class="font-semibold bg-transparent text-wrap relative  input input-bordered focus-within:outline-none focus-within:border-2 focus-within:border-black !outline-none border-2 border-black flex items-center gap-2 w-full p-4 max-w-xs text-white text-sm">
             Email: 
             <input type="text" name="email" id="">
         </label>
        <label for="" class="font-semibold bg-transparent text-wrap relative  input input-bordered focus-within:outline-none focus-within:border-2 focus-within:border-black !outline-none border-2 border-black flex items-center gap-2 w-full p-4 max-w-xs text-white text-sm">
         Password: 
         <input type="password" name="password" id="">
        </label>
        <a href="/signup" >For Signup</a>
         <input class="bg-zinc-950 mb-2 cursor-pointer px-8 py-2 rounded-md border-2 border-zinc-800" type="submit" value="Login">
     </form>
    </div>
 </body>
</html>